---

---

<div
  class="w-full h-full flex items-center justify-center bg-black overflow-hidden relative"
>
  {/* Placeholder for Dome Gallery - simulating the visual effect with CSS */}
  <div
    class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-gray-800 via-black to-black opacity-50"
  >
  </div>
  <div class="grid grid-cols-6 gap-4 opacity-30 transform rotate-12 scale-125">
    {
      Array.from({ length: 24 }).map((_, i) => (
        <div class="w-32 h-48 bg-gray-700/50 rounded-lg border border-gray-600/30" />
      ))
    }
  </div>
  <div
    class="absolute inset-0 flex items-center justify-center text-white/10 text-4xl font-bold"
  >
    Dome Gallery (Placeholder)
  </div>
</div>
